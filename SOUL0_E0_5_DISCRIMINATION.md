# SOUL0_E0_5_DISCRIMINATION.md — Proto-0 内部差异审计

> 性质：审计，不新增任何机制。`soul0/proto0.py` 动力学零改动（git 可证：本提交对动力学
> 文件无 diff）；新增仅为分析脚本 `soul0/e05_discrimination.py` 与结果文件。
> 目的唯一：确认 E0 是否已经存在**"同等外界扰动 → 因自身组织结构不同而产生不同内部
> 暂态响应"**。日期：2026-09-13。状态：待审查。
> 配套修正：SOUL0_NORMATIVITY_SURVEY.md 两处定义修正（B 门槛=暂态差分而非打破终局性；
> C 收紧=内部差异改变自维持关系本身的运行方式，构成性响应不算）。

---

## 1. 方法

- **配对构造**：从平衡点 (a₂, a₂, m*) 出发，施加**总侵蚀量相同**的扰动，仅改变其在
  a/b 间的分布（侵蚀为质量守恒的惰性转化，同 E0 W3）。
  - 对称分布：a、b 各失去 Δ/2；
  - 单侧分布：全部 Δ 从 b 扣除（Δ ≤ b₂=0.4208 时可行）。
- **比较量**：全程 a、b、m；再生通量 p(t)=k·a·b·m；p 的时间积分 ∫p；恢复时间
  （回到 0.9·a₂ 且不再跌落）；恢复/消散判定；暂态轨线差分 D(t)=‖x_sym(t)−x_asym(t)‖。
- **扫描**：预算 × 分布（固定 Δ=a₂ 的分布扫描；固定"对称 vs 单侧"的预算扫描）。

## 2. 关键配对（总侵蚀 Δ = a₂ = 0.42078，恰为组分总存量的 50%）

| 量 | 对称分布（各失 0.2104） | 单侧分布（b 失全部） |
|---|---|---|
| 最终命运 | **恢复**（回到平衡点） | **消散**（a,b → 0） |
| min a / min b | 0.2104 / 0.2104 | 3.9e-14 / 0 |
| 再生通量谷 p_min | 0.0769 | **0（瞬时关断）** |
| 通量积分 ∫p | **12.72** | **0.00** |
| 恢复时间 | **4.02 拍** | ∞ |
| 轨线差分 D(t) | 起点 0.2975 → 峰值/终值 **0.5951**（命运永久分岔） | |

**同一世界、同一总扰动量，仅因损伤在自身组分间的分布不同，内部暂态出现宏观差分，
最终命运相反。**差分的来源可以指认：互催闭合的位置性——再生项 k·a·b·m 是二次的，
b 清零的瞬间组织的整个再生引擎关断（∫p=0），而对称损伤只把通量压低 9%。

## 3. 两个扫描

- **固定预算 Δ=a₂ 的分布扫描**：split（预算中取自 b 的比例）∈[0.025, 0.975] 全部恢复；
  split∈{0, 1}（全部落在单条腿上）消散。镜像结构：每条腿只要保留约 2.5%（绝对量
  ≈0.0105）即可被另一条腿拉回——门槛条件实测为"未受损腿存量 > λ/(k·m_post)"
  （再生率转正的条件），数值与解析一致。
- **预算扫描（对称 vs 单侧）**：预算 ≤0.40 时两种分布都恢复（暂态差分随预算线性增大，
  峰值差分 0.07→0.30）；预算 ≥≈0.41 起出现**命运分化**（对称恢复、单侧消散）——
  与 E0 的非对称边界（97.5%–99.9% 之间）一致。

## 4. E0.5 三问的回答

**一、Proto-0 已经有什么？**
1. A 层（E0 已证）：存在域与存在边界的盆结构。
2. **组织结构产生的暂态差分响应（本审计新证）**：同等总外界扰动，仅因分布不同，
   内部暂态在组分谷值、再生通量谷、通量积分、恢复时间上全面分岔，直至最终命运相反。
   差分由互催闭合的**位置性**（损伤落在闭合的哪条腿上）产生，全程无观察者标注。
3. 响应的结构不对称性：对称损伤缓（通量压低 9%），单侧全损脆（通量瞬时归零）——
   组织"感受"扰动的方式依因果位置而不同。

**二、仍然缺什么，才不能称"这件事对它自身重要"？**
按收紧后的 C 定义：E0 的自维持规律 k·a·b·m 是**刚性**的——一切内部差异只能让状态
沿这条固定向量场滑动（构成性响应），没有任何内部差异能**改变自维持关系本身的运行
方式**（例如再生关系的有效强度/形式随内部状态变化）。差分响应因此只是"损伤的物理
后果"，组织没有以任何方式**使用**这种区分。缺的就是这一层。

**三、是否真的需要持久痕迹/记忆才能跨下一步？保持开放。**
两条路线都可想象，不预判：
- 慢变量/持久痕迹路线（痕迹累积成组织运行的内参数）；
- 无记忆路线（内部状态的快速差异直接调制关系的运行强度——不需要把区分保存下来，
  只需要让区分参与决定关系怎么跑）。
E0.5 的数据对两条路线均无偏向，留待下一阶段设计裁决。

## 5. 命名禁令确认

本审计的全部结论只使用表述：**"Proto-0 已具有组织结构产生的暂态差分响应。"**
不称其为 normativity、价值、关心、意识、记忆、主观性——任何一种称呼都超出了
A→暂态 B 的实测内容。

## 6. 产物

- `soul0/e05_discrimination.py`（分析脚本；proto0.py 未改动）
- `results/soul0_e05/`：pair_timeseries.csv、distribution_scan.csv、budget_scan.csv、
  figure_e05.png（配对暂态、通量对比、分布扫描、预算-差分曲线）
