# SOUL0_ATLAS0_FINDINGS.md — 状态空间图谱观察记录（v3，二轮审查修正后）

> 2026-09-13。设计：SOUL0_ATLAS0_DESIGN.md；实现：soul0/run_atlas0.py（v3）+
> soul0/atlas0_analyze.py（v3）。**只记录观察事实，不解释成认知、记忆、价值、器官或
> 主体结构。**区域使用无意义编号。
> 版本史：v1 的体素整数 key 比较（0.89–0.90 / 0.05–0.06）与 v2 的"同点一致率"
> （0.877–0.916，(2ia,2ib,2ir) 映射未做物理缩放）**均撤回**；Map B 自 v2 起从各 ε
> 自己的不动点起步，产物全部重算。

## 一、执行规模与可复现产物

- Map A（Formal）：每 ε coarse 66,300 + medium 515,100 点（free-running T=150 分类；
  网格与动力学未变，v2 起复用既有产物并在日志注明）。
- Map B（Reachable）：每 ε 从**该 ε 自己的 R1 正不动点**出发（自由运行求根、导数验证
  |dX/dt|max≤2.2e-14）：+0.25 → (0.4360, 0.4360, 0.1281, R*=4.3597)；
  0 → (0.4208, 0.4208, 0.1584, 4.2078)；−0.25 → (0.3971, 0.3971, 0.2058, 3.9710)。
  各 21×21 质量守恒侵蚀、T=300，记录 1,654,191 样本/ε。
- 全部 FINDINGS 数字来自 `results/soul0_atlas0/` 的 `atlas0_repro.json`、
  `atlas0_analysis.json`、`flip_surface_eps*.csv`、`gap_bands_eps*.csv`。

## 二、coarse→medium 收敛（v3 物理坐标口径）

| ε | 最近物理网格一致率¹ | 局部多数一致率² | 边界投影 Jaccard³ |
|---|---|---|---|
| +0.25 | 1.0000 | 0.9436 | 0.558 |
| 0 | 1.0000 | 0.9487 | 0.593 |
| −0.25 | 0.9999 | 0.9565 | 0.612 |

¹ 每个 coarse 点的实际物理 (A,B,R) 换算到**最近 medium 网格索引**（A/B 用 n=100，
R 用 medium 真实 100 点间距、同一 rmax；两种 R 网格非严格嵌套，故不称"同点"）。
一致率 ≈1.0 说明 coarse 分类在 medium 分辨率下逐点复现。
² medium 在粗网格一个间距邻域（27 体素）内多数结局 vs coarse 结局——**分辨率收敛
主指标**：0.94–0.96；不一致集中于 Boundary-01 邻域。
³ 边界点集（与合法邻居结局不同）投影到统一物理体素（边长 = 2 个 medium 间距）后的
Jaccard：0.56–0.61。

**判定：主要结构在加密下稳定；边界位置大体收敛。未触发自适应加密条件。**

## 三、Atlas 时程复核（真实 boundary 定向采样，v3 新口径）

从 medium fate 网格提取**真 boundary 点**（至少一个合法邻居结局不同；
总数 37,104 / 36,700 / 35,984），随机抽 2000 + 非 boundary 对照 1000，
T=150/600/2400 重分类：**三个 ε 的漂移率均为 0.00%**（150→600、600→2400、
150→2400；boundary 组与 control 组分别统计亦为 0）。T=150 分类时程对本图谱有效。

## 四、观察到的结构（编号沿用）

### Obs-1：Formal 空间 = 一个大恢复区 + 两个消散区
恢复占比（coarse/medium）：+0.25 → 90.7%/93.8%；0 → 89.7%/92.8%；−0.25 → 88.3%/91.0%。
**Region-D1**=A+B→1 斜边薄板；**Region-D2**=鞍点以下低质量区（a₁≈0.079，E0 解析）。
恢复区内无孤岛/镶嵌。

### Obs-2（Boundary-01）：命运翻转的位置证据与几何措辞（降级）
(A+B,R) 分箱翻转扫描（≥20 样本/箱）：ε=+0.25 恰有一个干净翻转箱
**(A+B≈0.158, R≈2.60)**（n_clean=1/57）；ε=0 与 −0.25 在该投影下无干净翻转箱。
E1（对称侵蚀、仅对称线上的态）在 ρ∈[0.76–0.85] 带发现 r 方向命运分岔。
两组证据**与"翻转结构局限在对称线附近的窄区域"相容；其三维几何形状尚未解析**
——本轮未做三维定向加密，不命名 tube/面/板。

### Obs-3（Gap-01，协议相对口径）：归一化距离三档敏感性
距离在归一化坐标 (A, B, R/rmax) 下度量，单位 = medium 网格间距，三档
（1×/2×/3×）。**只有三档下均 ≥99.5% 未访问的带才列为 Gap-01 候选**：
- **A+B≥0.1 且 0≤R<1：4/4 个质量带全部当选（三个 ε 一致）**；
- A+B≥0.1 且 1≤R<3：+0.25 → 4/4；0 与 −0.25 → 2/4（靠近鞍点的两个质量带
  在 3× 尺度下有历史访问，未入选）。
措辞限定：以上为"**当前协议**（各 ε 平衡态 → 单次质量守恒侵蚀 → 自由运行 T=300）
下未访问/低访问区域"，**不称"历史不可达/无法经过"**——绝对断言需数学证明或
更广历史协议。候选解释（未验证）：质量恢复与 r 恢复经由 p=G·k·A·B·M 同步变化。

### Obs-4：可达包络随 ε 幅值缩放，形状不变
可达包络 A,B 上限 0.493/0.460/0.416、R 上限 4.486/4.299/4.019（+/0/−）；
形状三 ε 一致，未观察到拓扑差异。

## 五、未观察到 / 不声称

未观察到：恢复区内部多稳态孤岛、可达集分叶、除 Boundary-01 外的第二结局边界、
随分辨率消失复现的瞬态结构。不声称：任何功能含义、任何认知/记忆/价值关联、
任何绝对不可达断言、Boundary-01 的三维形状。局限：T=150 依赖 §三 复核；
Map B 单次侵蚀协议限制可达覆盖；切片图仅取代表档位。

## 六、产物清单

`results/soul0_atlas0/`：formal_coarse_*.csv ×3、formal_medium_*.npz ×3、
reachable_*.npz ×3（含各 ε 不动点）、flip_surface_*.csv ×3、gap_bands_*.csv ×3
（含 1×/2×/3× 三档 far 比例）、atlas0_repro.json、atlas0_analysis.json、
atlas_eps*.png ×3、atlas3d_eps*.html ×3（**离线交互式 3D**，
include_plotlyjs=True，无网络可拖动缩放）。
