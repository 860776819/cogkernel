# SOUL0_ATLAS0_DESIGN.md — 状态空间图谱（第一阶段）

> 性质：观察性测绘。不新增任何动力学机制、不加噪声/学习/环境/reward/目标/新变量。
> 唯一目标：**观察当前 R1 系统的状态空间本身存在什么稳定结构。**
> 不证明 normativity、不找"脑区"、不给区域起功能名字（只用 Region-01/Boundary-01/Gap-01
> 等无意义编号）。日期：2026-09-13。状态：待审查。

---

## 一、坐标、合法域与 R 的前向有界推导

状态 (a,b,m,r)，守恒 A+B+M=1 ⇒ 以 **(A, B, R)** 为三维坐标，M=1−A−B。

**合法域**：A≥0，B≥0，A+B≤1，R≥0（(A,B) 构成直角三角形，面积 1/2）。

**R 前向有界推导**（不拍脑袋）：

1. 合法域内 A·B·M 的全局上界：max A·B·(1−A−B)。一阶条件给 A=B=M=1/3，
   上界 = **1/27**（≈0.037037）。
2. 再生通量 p = G(R)·k·A·B·M ≤ k·G_sup/27，其中 G(R)=1+ε·tanh(R/r₀) 在 R≥0 上的
   上确界依 ε 符号而定：
   - ε>0：G_sup = 1+ε（R→∞ 渐近）；
   - ε≤0：tanh(R/r₀)≥0 故 G(R)≤1，G_sup = 1（在 R=0 取到）。
3. 于是 dR/dt = p − μR ≤ k·G_sup/27 − μR，故
   **R > R̂ := k·G_sup/(27·μ) 时必有 dR/dt < 0**。
   - ε=+0.25：R̂ = 3×1.25/(27×0.02) = **6.944**
   - ε=0 与 ε=−0.25：R̂ = 3×1/(27×0.02) = **5.556**
4. 前向不变性：p ≤ μR̂ 恒成立，故 R=R̂ 处 dR/dt≤0——从任意 R₀≤R̂ 出发，R 不会越过
   R̂（一阶标量无过冲）；R₀>R̂ 则单调衰减回 R̂ 以下。
5. **采样范围**：各 ε 取 [0, R̂(ε)×1.05]（留边界余量）：
   ε=+0.25 → [0, 7.29]；ε=0、−0.25 → [0, 5.83]。R₀>R̂ 的合法初态仅作对照点少量采样
   （预期单调衰减，不计入主图谱统计）。

## 二、两张地图（绝不混合）

**Map A：Formal State-Space Atlas（数学合法即可采样）**
- 网格：A、B 各 n+1 点均匀分布于 [0,1]，保留 A+B≤1 的点；R 各 n 点均匀于 [0, R̂×1.05]。
  名义分辨率 coarse n=50（合法点 ≈6.6 万）、medium n=100（≈51.5 万）。
- 每点 **free-running**：不钳制 R、不加噪声、途中不干预。分类时程 T=150（依据：E1 已证
  fates 对 T=150/600/2400 数值稳定）、dt=0.02，RK4，分块批模拟。
- 记录：初始坐标；长期结局（恢复=min(a,b)≥0.5·a₂ / 消散）；恢复时间（首次 min(a,b)≥0.9a₂）
  或消失时间（首次 min(a,b)<θ=0.05，若结局为消散且起点高于 θ）；末态；邻近点结局差异。
- 语义：**"数学上可以从这里开始会怎样。"**

**Map B：Reachable-History Atlas（只能靠历史走到）**
- R **不能任意设置**。出发点：E0/E1 平衡态经**已定义的质量守恒侵蚀扰动**
  （d_a、d_b 各占 a₂ 的比例，21×21 网格覆盖 [0,1]²），r₀=平衡值 λa₂/μ≈4.2078。
- 当前 R1 自由运行 T=300，每 4 拍记录 (A,B,R)（≈441 轨迹 × 76 样本/ε）。
- 语义：**"Proto 靠自己的历史实际走到过哪里。"**
- **Formal ∩ Reachable 的差集单独报告**（数学合法但历史长期未访问的区域）——
  这正是"数学合法 ≠ 历史可达"的实证。

## 三、三个 ε 独立观察

ε=+0.25 / 0 / −0.25 各自独立生成 Formal（coarse+medium）与 Reachable 图谱。
不评价优劣，不按"更能活"选择。

## 四、收敛验证协议（先于任何大扫描）

1. coarse → medium 对比：
   - 大区域结局分类稳定性（coarse 网格单元多数结局 vs medium 同位置结局的一致率）；
   - 结局边界位置是否趋于一致（边界单元集合的 Jaccard 相似度）；
   - 反复出现的空洞/薄层/弯曲/折叠/孤立区域；
   - 哪些结构随加密消失、哪些持续。
2. 仅对结局发生变化的邻域做**自适应加密**（局部细化，不全局翻倍）。
3. 第一阶段总量控制在 ≈百万级以内；**若 coarse→medium 主要结构趋稳，再决定是否
   百万/千万级精细扫描**（本轮不做）。

## 五、承认的结构现象（先于命名）

只认这些：不同长期结局的稳定区域；稳定结局边界；收敛时间的稳定梯度/层；邻近初态
未来迥异的敏感区；Formal 存在但 Reachable 长期为空的区域；多采样密度下重复出现的
几何结构。全部使用无意义编号。**若地图没有稳定结构，如实记录"当前系统未观察到
值得进一步分区的稳定几何结构"。**

## 六、可视化（每 ε 至少）

1. (A,B,R) 三维状态图（按结局着色，子采样）；2. A-B 切片（固定 R 档位）；
3. A-R 切片（固定 B 档位）；4. B-R 切片（固定 A 档位）；5. Formal vs Reachable 对照；
6. coarse vs medium 对照。颜色仅表示客观分类（结局/到达与否），禁用
"健康/危险/好/坏"等语义色。

## 七、产物

`SOUL0_ATLAS0_DESIGN.md`（本文）、`soul0/run_atlas0.py`、`results/soul0_atlas0/`
（各 ε 的 fate 网格 CSV、边界清单、reachable 占用、图）、`SOUL0_ATLAS0_FINDINGS.md`
（只记观察事实）。
