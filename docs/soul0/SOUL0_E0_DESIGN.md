# SOUL0_E0_DESIGN.md — Proto-0：最小存在组织

> 性质：E0 实验设计。研究选择已由研究者作出：**对单个 Proto，存在定义是天生携带的硬性条件**
> （先天给出，不是学习结果），但**存在定义 ≠ 生存策略**——本实验不告诉 Proto 怎样避免消散、
> 什么有利有害、该做什么、该最大化什么。
> **E0 没有行为层**：无感知、无动作、无学习、无记忆、无概念。要检验的命题只有一条：
> **这个组织的持续存在，是否由其内部相互维持关系成立；切断关系，同样的世界条件下组织消散。**
> 日期：2026-09-13。完成后不扩展 E1。

---

## 1. 最小模型

原体 = 两个组分 **a、b** 与惰性池 **m** 的动力学。总质量 N = a+b+m 守恒（取 N=1）。

**世界规则**（作用于一切构型，永不修复原体）：

- **W1 惰性转化**：每个组分以速率 λ 自发转化为惰性形式 m：`a → m`，`b → m`。
- **W2 有限介质**：总质量守恒（有限世界）。
- **W3 侵蚀事件**：世界可对组分施加均匀侵蚀（按比例 ρ 移除指定组分）——由实验者在指定
  时刻触发，属世界级扰动规律，对任何构型一视同仁。

**原体先天结构**（存在组织 R，诞生即携带，永不学习）：

- **P1 互催再生**：每个组分可由 m 再生，但该再生过程**只在另一个组分在场时运行**：
  ```
  da/dt = -λ·a + k·a·b·m
  db/dt = -λ·b + k·a·b·m
  dm/dt = +λ·(a+b) - 2·k·a·b·m
  ```
  a 的维持依赖 b（b 催化 a 的再生），b 的维持依赖 a——**互为维持条件**。

**观察者仪器**（只用于测量，绝不进入动力学）：存在判定阈值 θ、恢复/消散分类、
观察时程、延拓时程。这些阈值**不影响任何导数**——E0 无 `if dead`：
不存在任何死亡标志位；组分归零不是被宣布的，是动力学自己去的。

参数取值（研究者选定，属先天结构的数值部分）：λ=0.2，k=3，N=1。

### 动力学推演（预注册预期，实验来测，不是事后拟合）

- 可持续不动点：对称解 a=b=a*，λ = k·a·(1-2a) ⇒ 两根：
  **a₁≈0.0792（鞍点，不稳定）**、**a₂≈0.4208（稳定，m*≈0.158）**。
  可持续区域存在的前提：k·N² > 8λ（否则不存在任何正不动点，见审计 A-λ/A-k）。
- 由对称性，差模 (a−b) 以速率 λ 衰减；约化和模动力学 S=a+b 近似服从
  `dS ≈ -λS + (k/2)S²(1-S)`，其不稳定和阈 **S₁≈0.1584**（=2a₁）。
- **涌现边界预测**：均匀侵蚀 ρ 后 S'=(1-ρ)·S₂，恢复 ⇔ S' > S₁
  ⇒ 临界 **ρ* ≈ 1 − S₁/S₂ ≈ 0.812**。此边界是动力学的产物，不是规定的阈值。

## 2. 变量与规则审计（逐项：删掉它，实验问题还成立吗？）

| 项 | 为什么必须存在 | 删除后实验问题是否仍成立 |
|---|---|---|
| 组分 a、b | 存在组织的承载者 | **删一个**：只剩单组分自催化 `da=-λa+ka²m`——存在域与分离线仍在，但"内部关系"退化为自环：没有任何**其他**过程在维持它，违反 E0 宗旨（互依赖必须有"他者"）。**都不删也不加第三个**：三组分不回答任何二组分回答不了的问题 |
| 互催形式 k·a·b·m | 组织的全部内容 | 改为线性互催 `k·b·m`（a 的再生只需 b 在场、不需 a 自己）：则 (0,0) 从鞍点退化为"仅精确零点吸收"，任何正构型都恢复——**不可逆消散在动力学上不再可实现**，问题 8/10 失效。改为自催化 `k·a²·m`：互依赖消失，同单组分问题。故**必须**是要求双方在场的二次互催 |
| 惰性池 m | 衰减的受体+再生的底物 | 删 m（再生凭空造物）：破坏世界守恒，且再生速率与存量无关→恢复过易、消散难定义；删守恒（m 无限）：无饱和、无可持续区域，只有失控增长或衰减——问题 6 失效。m ≠ food：无任何价值语义，它是组分的惰性形式；把它变回组分的是原体自己的过程，世界从不做这件事 |
| λ（惰性转化率） | 消散压力 | 删（λ=0）：nothing 衰减，"维持"失去对象，存在退化为平凡常驻——问题 5/8 失效 |
| k（再生率） | 关系的强度 | 删（k=0）：只剩衰减，无组织可言；k·N²≤8λ：可持续区域不存在，问题 6 失效。参数值是先天结构的数值部分，**现象**（可持续域、分离线、不可逆消散）是动力学的 |
| W3 侵蚀事件 | 扰动源 | 删：只剩正常运行，问题 7/8（可恢复/越界）无从检验 |
| 对称性（a、b 同 λ 同 k） | 极简选择 | 非必需假设；取对称是为少两个参数。不对称参数下分离线仍在（位置移动），本 E0 不检验此稳健性 |

**FOUNDATIION §5 偷渡自查**：无 reward、无 death_bad、无好奇/情绪/人格/思想模块、无 LLM、
无预标签对象；m 已声明为非 food；无任何"应该"变量。唯一的"研究者规定"就是存在组织本身
（互催结构与其参数）——这正是本轮研究选择所允许并要求记录的先天层。

## 3. 实验组

| 组 | 设置 | 预注册预期 |
|---|---|---|
| E-正常 | 平衡初值，长时运行，无侵蚀 | 收敛到 (a₂,a₂,m*)；可持续区域存在 |
| E-可恢复 | t=25 均匀侵蚀 ρ=0.5（a、b 同时） | 短暂下探后自行回到 a₂ 邻域（ρ<ρ*） |
| E-非对称 | t=25 仅侵蚀 b 的 90% | 恢复（环路可重建一条腿）。（预注册原预测"99.9% 仍可恢复"**被实验否证**，见 §6） |
| E-越界 | t=25 均匀侵蚀 ρ=0.99 | S'≈0.0084 < S₁，动力学滑向 (0,0)；**延拓 10 倍时程**：a、b 单调趋于 0 且再生率恒小于衰减率——不可逆消散由未来动力学证明，无死亡标志位 |
| C-切断（关键对照） | 同一平衡初值、同一世界，互催再生过程停用 | 单调衰减至惰性池。**若此组长期保持，则"自身维持"是假象** |
| C-半切（补充） | 只停用 b 的再生（a 仍靠 b 催化再生） | b 衰尽后 a 的再生随之消失→相继消散：闭合需要**两个方向** |
| 扫描 | ρ∈[0.05,0.999] 均匀侵蚀，各跑全程 | 分界的解析值 ρ\*=1−a₁/a₂≈0.8118（对称下约化精确）；40 点粗扫描只能夹住它：**[0.8043, 0.8287]**（0.8043 恢复、0.8287 消散）——粗网格首个失败采样点不是边界值 |

## 4. 判定与证伪

- "组织在场"（观察者仪器）：a>θ 且 b>θ（θ=0.05）；"恢复"= 侵蚀后回到 ≥0.5·a₂；
  "不可逆消散"= 延拓时程内 max(a,b) 持续低于阈值且再生/衰减比不反转。
- **证伪条件**：C-切断组若长期保持 → "自身维持"指控成立，E0 结论反向；
  扫描若无清晰分界 → 存在域概念在此组织上不成立。
- 全部结果无论好坏都保留。

## 5. 产物

`soul0/proto0.py`（世界/原体/观察者仪器三段式）、`soul0/run_e0.py`（七组实验+扫描+图）、
`results/soul0_e0/`（时序 CSV、扫描 CSV、figure_e0.png：内部变量时间序列、侵蚀时刻、
恢复或消散的轨迹、切断对照、涌现分离线）。

## 6. 结果补记（E0 跑完后回填；含一次设计修正与一次被否证的预测）

**设计修正（实现层）**：W3 初版实现为"湮灭"（被侵蚀质量凭空消失），与 W2 守恒矛盾
——m 缺质量导致再生无底物、ρ=0.5 即消散。修正为**强制惰性转化**：被侵蚀质量转入 m
（与 W1 同一种规律、只是强度更高，不发明新规律种类）。此修正前的一切数字作废。

**否证记录**：预注册预测"非对称 99.9% 侵蚀一条腿仍可恢复"是**错的**。实测：99.9% 单侧
损失 → 整体消散；90% → 恢复；扫描测得非对称边界在 97.5%–99.9% 之间。
原因（修正后的分析）：a 的衰减 e-fold ≈ 5 拍，而 b 从 0.04% 重建需要 ~13 个 e-fold——
**再生的催化剂就是那条正在挨饿的腿**，重建竞赛跑不过催化者的衰变。
教训：再生速率受存量二次瓶颈（k·a·b·m）限制的组织，对单侧"近乎全损"比对均匀损伤更脆弱
（均匀分界解析 0.8118；非对称分界 ≈0.98+）——存在域不是积集合，有真实形状。

**最终数字**（λ=0.2, k=3, N=1）：

| 组 | 结果 | 判定 |
|---|---|---|
| E-正常 | 收敛 (0.42078, 0.42078, 0.15844)，与解析 a₂ 一致 | 可持续区域存在 ✓ |
| E-可恢复 ρ=0.5 | 下探后自行回到平衡 | 存在域内 ✓ |
| E-非对称 ρ=0.9 | 恢复 | 存在域内 ✓ |
| E-越界 ρ=0.99 | 滑向 (0,0)；延拓 800 拍 max(a,b)=7.5e-08 且仍单调下降 | 不可逆消散由未来动力学证明 ✓（无死亡标志位） |
| C-切断 | 同世界下单调衰减至零 | 组织的持续**确实**依赖内部关系（关键对照通过） |
| C-半切 | 衰减至零 | 闭合需要两个方向 ✓ |
| 扫描 | 对称分界：解析 **ρ\*=1−a₁/a₂≈0.8118**，40 点粗扫描夹于 **[0.8043, 0.8287]**（0.8043 恢复、0.8287 消散）；非对称分界 ρ*≈0.98–1.0 | 存在域有形状、被解析与数值双重刻画 ✓ |

**能力边界（E0 明确不声称的事）**：Proto-0 当前验证的是——**固定先天存在规则下，
内部耦合过程能维持组成状态，并自然形成恢复盆与消散盆**。它尚未验证——**原体能够维护、
再生产定义其自身的存在规则本身**：P1 互催规律 k·a·b·m 仍是研究者固定写入的先天动力学规则，
它不会损坏、不需要被原体修复。这是 E0 的明确边界，不算缺陷，不在本轮解决。
