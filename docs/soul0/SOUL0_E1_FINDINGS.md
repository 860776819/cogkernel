# SOUL0_E1_FINDINGS.md — R1 调节自由度：实现与证伪实验结果

> 2026-09-13。设计文档：SOUL0_E1_REGULATION_DESIGN.md（预注册常数：μ=0.02、
> ε∈{+0.25, 0, −0.25}、r₀=λ·a₂/μ≈4.2078；实验中未调整任何参数）。
> 实现：`soul0/proto1.py`（4 维动力学，E0 守恒律保持）、`soul0/run_e1.py`（验证电池 A–H）。
> 产物：`results/soul0_e1/`（3 个 fate_scan CSV、matched_states.csv、figure_e1.png）。

## 判定：E1 五条最低判据全部通过

| # | 判据 | 证据 |
|---|------|------|
| 1 | r 由实际组织过程产生 | 源项=p_actual=G(r)·k·a·b·m，实现中无任何反事实量（k·a·b·m 未调制版本已按审查淘汰） |
| 2 | r 不是当前 (a,b,m) 的即时函数，有自身动态与衰减历史 | 4 维状态空间（E0 为 3 维）；C：两条不同历史在 (a,b,m) 重合到 <0.01 时 |Δr| 仍=0.065（ε=+0.25）/0.164（ε=−0.25），随后按 μ 时间尺度衰减（150 拍后 0.0044/0.0099）——**history-dependent fading trace** |
| 3 | r 反向改变构成性自维持过程 | matched-state 干预：初导数差分与引理预测吻合至三位小数（ε=+0.25: 预测 −0.00033 / 实测 −0.00032；ε=−0.25: +0.00033/+0.00033） |
| 4 | same a,b,m / different r 自由运行轨迹差异可验证 | D：发散持续 2.9–4.7 拍（平衡态附近则 <1 拍收敛）；**F：命运分岔真实存在**（见下） |
| 5 | 结论不依赖"有利生存"的反馈符号 | H：±ε 都出现结构现象且**方向相反**；ε=0 完全无现象（控制） |

## 主要结果

**A（回归控制）**：ε=0 时与 E0 轨迹逐点差=0.00e+00（位级一致），质量误差 2.9e-15；
r 独立变化 [4.198, 4.291] 但不影响 a,b,m——实现正确性硬检查通过。

**B（镜像）**：三 ε 镜像违背 ≤2.66e-15（机器精度）——R1 镜像不变性数值确认。

**D+G（自由度存在的动力学签名）**：不动点 Jacobian 谱含**1 个守恒零模**（质量守恒，
|Re|<1e-4，中性）与一个**慢非零模**（排除零模后实部最接近 0 的稳定特征值）：
ε=+0.25: **−0.01973，r 方向权重 0.994**；ε=0: **−0.02000，权重 1.000**（未耦合积分器
精确值）；ε=−0.25: **−0.02086，权重 0.981**。慢特征值预测以 <5% 偏差验证；非零特征值
全实（无振荡）。稳定性口径：**在固定总质量 N=1 的守恒流形内稳定；完整 4D 系统携带
守恒中性零模**。不动点口径：**在 N=1 守恒流形内，本次搜索只发现一个正的非零不动点**。
（早期报告的 r 权重 ~0.81 系 slow-mode 选择未排除零模所致，已修正并重算。）

**F（free-running 命运分岔——本轮核心结果）**：在 frozen-r 诊断指出的边界带
（ρ∈[0.70,0.90]）内，41 个对称侵蚀态 × 自然 r 范围 5 档的全扫描：

| ε | 命运分岔态 | 方向 |
|---|-----------|------|
| +0.25 | **6/41**（ρ∈[0.82, 0.845]） | 低 r 消散，高 r 恢复 |
| 0 | **0/41** | 无自由度效应（G≡1，控制） |
| −0.25 | **10/41**（ρ∈[0.76, 0.805]） | 低 r 恢复，**高 r 消散**（调节有害区） |

两个符号下都存在真实的命运分岔，**方向随符号翻转**——结构现象来自"存在调制自由度"
本身，不来自研究者选的保护方向。ε=−0.25 的分歧区正是调节有害区：同一 (a,b,m)，
r 高的克隆死、r 低的克隆活——这证明系统没有内置"趋生性"，同时证明了调节自由度
对存在命运的因果作用。

**长时程复核（F 补充，参数与 r grid 不变）**：对命运分岔区以 T=150 / 600 / 2400 三个
时程重新分类：ε=+0.25 分歧集 {0.820…0.845}、ε=−0.25 分歧集 {0.760…0.805}、ε=0 空——
**三个时程下均无漂移**。结论：**free-running fate bifurcation 对 T=150/600/2400 数值
稳定**；不称解析边界，不作数学证明。
（归档修正注：natural_r_range() 曾将 budget 误写死为 0.420782，修复后 r 范围与 F 结论
不变；上述数字均为修复后重跑所得。）

## E0.5 归档校正补充

数值分界 [0.00996, 0.01006]（split 口径）在 T=150/600/2400 三个时程下无漂移
（`results/soul0_e05/boundary_horizons.csv`）：长时程数值结果稳定；口径保持
"当前有限时程与末态阈值判定下的数值分界夹逼"，不称解析边界。

## 边界与不声称（重申）

- E1 证明的是**结构**：给定先天耦合关系（G、μ、ε、r₀ 由造物主定义），组织运行产生了
  一个内自产生、历史依赖、部分解耦、并反向调制自维持的自由度。
- **不声称**：normativity、价值、关心、欲望、意识、自主目标、记忆（r 是 fading trace，
  非记忆模块）、"Proto 自己发明了调节机制"（调节结构是先天的，r 的取值与历史才是
  Proto 自己的）。
- matched-state 干预由实验者实施（克隆+设 r）——这是测量操作；自由运行中 r 由
  历史自然到达。
- 已知局限：命运分岔仅在边界带（ρ∈[0.75, 0.85]）出现，远离边界时调节的影响收敛
  消失；自然 r 范围在正常工况下较窄（稳态附近 [4.2, 4.36]，全历史 [0.01, 4.36]）。

## 结论

E1 通过：在 R1 先天组织关系下，一个内自产生、历史依赖、部分动力学解耦、并反向
调制自维持过程的自由度**存在且起作用**——包括其有害作用的案例。这就是"构成性响应
之外存在真正的内部调节自由度"的最小证明，同时也是 normativity 问题的实验地基：
后续若研究该自由度与存在边界的差分关联，地基已经就位。
