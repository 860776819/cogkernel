# SOUL0_E1_REGULATION_DESIGN.md — 调节自由度：E1 设计（只设计，不实现）

> 文档性质：E1 设计文档。**只做设计与必要数学推演，不含实现代码。**
> E1 唯一问题：**"构成性响应"之外，能否存在真正的内部调节自由度。**
> 即：一个相对/部分动力学解耦、由 Proto 自身组织状态产生、有自己动态、并能反过来调制
> 构成性自维持过程的内部自由度。
> E1 **不声称**产生：normativity、价值、关心、意识、欲望、记忆、生存目标。
> 纪律：每候选须通过 §六 严禁清单自查；"反馈让 Proto 更容易活"**不是**成功条件
> ——哪怕调节有时有害，只要结构判据满足，E1 的结构问题即被回答。
> 日期：2026-09-13。状态：待审查。

---

## 一、E0.5 归档校正记录（step 0）

- E0.5 的 split≈0.00996–0.01006 全文改为："**当前有限时程（T=150）与末态阈值判定下的
  数值分界夹逼**"，不称真边界。
- 长时程复核（`soul0/e05_horizon_check.py`，T=600、T=2400）：夹逼区间**无漂移**
  （两端均为 [0.00996, 0.01006] / [0.98994, 0.99004]）。结论：**长时程数值结果稳定**；
  仍不宣称严格解析边界。产物：`results/soul0_e05/boundary_horizons.csv`。

## 二、调节自由度的形式判据

设原体状态为 (a,b,m)（E0 动力学）。一个合法的调节自由度 r 须同时满足：

1. **产生性**：dr/dt 的源项是 (a,b,m)（或其历史）的函数——由组织自身的状态/过程产生；
2. ** Own dynamics**：dr/dt 含 r 自身项（ own 衰减/饱和），r 在**当前 (a,b,m) 给定时
   仍不完全确定**（依赖历史）——因此 r 不是 a,b,m 的别名（形式理由：状态空间从三维
   增至四维；存在 (a,b,m) 相同、r 不同的两条真实历史，故 r ≠ f(a,b,m)）；
3. **部分动力学解耦**：r 有自己的时间尺度（1/μ，明显慢于组织弛豫 ~1/λ）与自己的衰减律
   ——不被 (a,b,m) 即时锁死（解耦），但源项与调制都闭环在组织上（耦合）。
   数学签名：系统在不动点处出现 E0 没有的**慢特征值（≈−μ）**；
4. **调制性**：r 进入构成性自维持项的**系数**（不是状态本身）：再生通量变为
   G(r)·k·a·b·m，即 r 改变自维持关系**运行得多强/如何分配**，而非仅改变位置；
5. **matched-state 干预判据**：任取状态使 (a,b,m) 完全相同、仅 r 不同（r₁≠r₂，
   G(r₁)≠G(r₂)），则 `d(a−a′)/dt|₀ = k·a·b·m·(G(r₁)−G(r₂)) ≠ 0`——**即时分岔可证**。
   强版本：把 matched state 放在分离线附近，存在 r₁ 下恢复、r₂ 下消散的配对
   ——**存在域本身成为 r 的函数**（ρ*(r)，解析式见 R1 推演）。

对"别名"与"构成性响应"的两条边界由此自动成立：不满足 2 的即时函数是别名；
不满足 4 的记录是仪表（E0.5 已有暂态 B，无 C——本 E1 补的正是 4）。

---

## 三、候选 R1：活动积分自由度（推荐）

- **造物主定义**：存在组织（同 E0）；新增状态 r 及其动力学
  `dr/dt = k·a·b·m − μ·r`（源=组织自身的再生通量，自有衰减 μ）；调制形式
  `G(r) = 1 + ε·tanh(r/r₀)`，ε、r₀、μ 为先天常数（数值实现前预注册，不得为结果调选）。
  动力学变为：
  ```
  da/dt = −λa + G(r)·k·a·b·m
  db/dt = −λb + G(r)·k·a·b·m
  dm/dt = λ(a+b) − 2·G(r)·k·a·b·m
  dr/dt = k·a·b·m − μ·r
  ```
- **Proto 运行后产生**：r 的全部取值。r 是组织活动水平的慢积分（μ=0.02 → 时间尺度
  ~50 拍，为组织弛豫 ~5 拍的 10 倍）；它的当前值由**历史**决定，不由当前 (a,b,m) 决定。
- **不是别名**：维度论证（4>3）+ 历史欠定论证（到达同一 (a,b,m) 的两条路径留下不同 r，
  数值上可直接展示）。
- **部分解耦**：自有时间尺度与衰减（谱上表现为 E0 没有的慢特征值 ≈−μ 附近）；
  与组织的耦合全部闭环（源读组织，出写组织）。
- **调制自维持**：G(r) 乘在构成性再生项上——r 高则自维持关系运行得强，r 低则弱。
- **matched-state 干预**：取 (a,b,m) 相同、r=r₁ vs r₂ 的两个克隆，同世界运行：
  即时分岔由上面的引理保证；强版本放在分离线附近——由推演，鞍点位置随 r 移动：
  `a₁(r) = [1 − sqrt(1 − 8λ/(G(r)·k))]/4`，故存在 (a,b,m) 使 r₁ 下消散、r₂ 下恢复，
  **存在域边界成为自由度的函数**。E1 的主测量即 ρ*(r) 曲线（钳制 r 的参数扫描）
  与 matched-state 命运配对。
- **数学推演要点**：质量守恒对任意 r 成立（W2 不破）；对称约化在 (x, r) 平面上为
  二维系统，不动点自洽方程 r* = λx*/μ、λ = G(r*)·k·x*(1−2x*)——可持续区域随
  先天常数移动（ε 的符号与大小改变 x*，是可测先验预测）；μ→∞ 极限退回 E0
  （连续性检验）。Jacobian 在不动点出现慢模（≈−μ），预测恢复暂态获得慢成分
  ——调节不免费，可能出现"调节过冲"甚至自伤案例（**必须如实报告**）。
- **最大偷渡风险**：ε 的符号。若以"恢复更好"为标准选 ε，则"关心"被写进耦合常数。
  对策：ε 预注册；分析对 ±ε 双跑（分岔判据对两种符号都成立，结构主张与符号无关）；
  有害案例必须呈现。

## 四、候选 R2：失衡分配自由度

- **造物主定义**：E0 的对称再生分配改为可调：a 得 ω(r)·2kabm，b 得 (1−ω(r))·2kabm，
  ω(r) = 0.5 + ε·tanh(r/r₀)；`dr/dt = κ·(a−b) − μ·r`（源=组分不平衡）。
- **Proto 运行后产生**：r（失衡的慢积分）与分配的实际走向；可产生两腿维持节奏的
  振荡式交替（自由度与组织互推）。
- **不是别名 / 部分解耦**：同 R1（积分器 + 自有时间尺度；4 维状态空间）。
- **调制自维持**：改变自维持产出的**分配**（哪条腿被维持得更快），总量不变。
- **matched-state 干预**：同 R1；注意对称流形上 dr=−μr → r→0 → ω→0.5，
  R2 在平衡态与 E0 **完全重合**——其效应只存在于非平衡暂态（这是结构特点也是
  验证难点）。
- **最大偷渡风险**：κ/ε 的符号组合里藏着"补短抑长"的恢复控制器——若选出的符号
  恰好是缺陷纠正型，normativity 断言虽不做，结构上也已把 homeostasis 从分配通道
  请回来。另：打破镜像对称使 E0.5 式的镜像校验失效，不透明性上升。

## 五、候选 R3：负荷-亲和自由度

- **造物主定义**：`dr/dt = λ·(a+b) − μ·r`（源=惰性转化负荷）；再生通量改为
  `k·a·b·m/(1+r/r₀)`（r 抬高对底物池的"亲和门槛"）。
- **Proto 运行后产生**：r（衰减负荷的慢积分）与组织-介质耦合的实际强弱。
- **不是别名 / 部分解耦**：同前（积分器、自有时间尺度、4 维）。
- **调制自维持**：调制组织从介质**抽取底物的方式**（亲和），而非速率或分配。
- **matched-state 干预**：同 R1（G(r) 进入通量，即时分岔引理同样成立）。
- **最大偷渡风险**：(a) 1/(1+r/r₀) 是酶动力学形状——地球模板偷渡；
  (b) "负荷高→再生低"是负反馈控制器的形状（homeostat-lite）；
  (c) 亲和是组织-介质**界面**参数，调制它模糊了世界规则与原体结构的边界
  ——需要额外论证这是原体侧的抽取过程而非世界律改动。

## 六、严禁清单 × 候选自查

| 禁止项 | R1 | R2 | R3 |
|---|---|---|---|
| survival reward / 生存目标 | 无奖励通道；有害案例必须报告 | 同 | 同 |
| death/alive 输入 | 无 | 无 | 无 |
| 距离存在边界的量 | r 源=活动通量（无边界语义）；审计点：μ、r₀ 不得反向凑出"距离" | r 源=不平衡；审计点同 | r 源=惰性转化负荷；审计点同 |
| 显式 danger 值 | 无 | 无 | 无 |
| "偏离即恢复" if/controller | 无 if；全光滑耦合 | 同 | 同 |
| 选"有利恢复"的反馈符号并称其内在重要性 | ε 预注册 ± 双跑；不称重要性 | 最大风险点（见 §四） | 次大风险点（负反馈形状） |
| 为调节预设记忆模块 | r 为一阶泄漏积分器（单标量、无读写内容），且 E1 不依赖"记住任何事"，只依赖"当下 r 影响后续" | 同 | 同 |

共同审计纪律：所有新常数（μ、ε、r₀、κ、K）在实验前预注册；matched-state 干预
（判据 5）与"± 符号双跑"为必做项；若只在保护性参数下才出现结构性结果，
结论一律改写为"研究者安装了保护"。

## 七、推荐：R1

R1 比另外两个更少偷渡、更容易证伪，理由：

1. **选择最少**：一个源（组织自身通量）、一个有界调制、一个时间尺度——
   R2 多出符号组合的控制器空间，R3 多出酶动力学形状与界面归属问题。
2. **可解析证伪**：镜像对称与守恒完整保留，对称约化在 (x,r) 平面精确成立，
   不动点方程、慢特征值、a₁(r) 边界公式都可先写后测——任何拟合痕迹一眼可见。
3. **结构主张与符号无关**：matched-state 分岔引理对 ±ε 同样成立——
   E1 的结构问题（调节自由度是否存在且起作用）不依赖"调节是否有利"，
   这正是它绕开 survival-reward 偷渡的关键。
4. **自带反例面**：活动积分的过冲可以自伤（G>1 加速消耗 m），
   报告有害案例即可证明系统没有内置"趋生性"。

R2 保留为 R1 之后的自然延伸（分配通道），R3 与其说候选不如说警示
（界面模糊 + 地球形状），本轮不推进。

## 八、明确不做

不实现（无代码）；不做 E1 之外的机制；不引入感知/动作/学习/记忆模块；
不在 E1 使用任何 normativity/价值/关心词汇作为结论；不为结果挑选 μ、ε、r₀。
