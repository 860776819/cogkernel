# SOUL0_ATLAS0_FINDINGS.md — 状态空间图谱观察记录（v2，审查修正后）

> 2026-09-13。设计：SOUL0_ATLAS0_DESIGN.md；实现：soul0/run_atlas0.py（v2）+
> soul0/atlas0_analyze.py。**只记录观察事实，不解释成认知、记忆、价值、器官或主体
> 结构。**区域使用无意义编号。本版取代 v1 草稿：v1 的 coarse→medium 比较方法无效
> （体素整数 key 对应不同物理位置），0.89–0.90 / 0.05–0.06 两组数字**撤回**；
> Map B 已改为各 ε 自己的不动点起步，旧 Reachable/Gap/Obs-5 结果**全部重跑，不沿用**。

## 一、执行规模与可复现产物

- Map A（Formal）：每 ε coarse 66,300 + medium 515,100 点（free-running T=150 分类；
  网格与动力学未变，v2 复用既有产物并在日志中注明）。
- Map B（Reachable）：每 ε 从**该 ε 自己的 R1 正不动点**出发（自由运行求根、导数
  验证 |dX/dt|max≤2.2e-14）：+0.25 → (0.4360, 0.4360, 0.1281, R*=4.3597)；
  0 → (0.4208, 0.4208, 0.1584, 4.2078)；−0.25 → (0.3971, 0.3971, 0.2058, 3.9710)。
  各 21×21 质量守恒侵蚀、T=300，记录 1,654,191 样本/ε。
- 全部 FINDINGS 数字来自 `results/soul0_atlas0/` 下的
  `atlas0_repro.json`、`atlas0_analysis.json`、`flip_surface_eps*.csv`、
  `gap_bands_eps*.csv`（由 run_atlas0.py / atlas0_analyze.py 生成）。

## 二、coarse→medium 收敛（物理坐标对齐，方法修正后）

三个互补指标（定义见 run_atlas0.compare_physical）：

| ε | 近点一致率¹ | 局部多数一致率² | 边界投影 Jaccard³ |
|---|---|---|---|
| +0.25 | 0.9155 | 0.9437 | 0.558 |
| 0 | 0.8989 | 0.9487 | 0.593 |
| −0.25 | 0.8770 | 0.9563 | 0.612 |

¹ medium 网格上与 coarse 点最邻近的物理点（R 坐标相差 <1% 量程）的 fate 一致率——
**不是**确定性检查（确定性系统同点必同轨）；不一致集中反映边界带的真实敏感性。
² medium 在粗网格一个间距邻域（27 体素）内的多数结局 vs 该 coarse 点结局——
**分辨率收敛的主指标**：0.94–0.96。
³ 边界点集（与合法邻居结局不同的点）投影到统一物理体素（=2 个 medium 间距）后的
Jaccard：0.56–0.61。粗细分辨率下边界带物理位置部分重叠；带宽度有限且细分辨率
边界点更稀疏，Jaccard<1 属预期。

**判定：主要结构在分辨率加密下稳定（局部多数一致率 0.94–0.96）；边界位置大体
收敛（投影 Jaccard 0.56–0.61）。未触发自适应加密条件。**

## 三、Atlas 自身的时程复核（新增）

从 medium 图谱抽样：边界带（A+B∈[0.05,0.30]）2000 点 + 随机对照 1000 点，
T=150/600/2400 重分类。**三个 ε 的漂移率均为 0.00%（150→600、600→2400、150→2400）**。
T=150 分类时程对本图谱有效（atlas0_repro.json: horizon_recheck）。

## 四、观察到的结构（编号沿用）

### Obs-1：Formal 空间 = 一个大恢复区 + 两个消散区
恢复占比（coarse/medium）：+0.25 → 90.7%/93.8%；0 → 89.7%/92.8%；−0.25 → 88.3%/91.0%。
**Region-D1**=A+B→1 斜边薄板；**Region-D2**=鞍点以下低质量区（a₁≈0.079，E0 解析）。
恢复区内无孤岛/镶嵌。

### Obs-2（Boundary-01）：命运翻转结构是贴着对称线的细管，不是质量带平面
atlas0_analyze 的 (A+B,R) 分箱翻转扫描（≥20 样本/箱）：ε=+0.25 恰有一个干净翻转箱
**(A+B≈0.158, R≈2.60)**（n_clean=1/57）；**ε=0 与 −0.25 在该投影下无干净翻转箱**。
解读（几何观察，非机制断言）：E1 发现的命运分岔限制在对称线 A=B 附近的细管内
（E0.5 已证非对称损伤结局截然不同），按 A+B 分箱时被大量非对称态稀释，
故投影下不成面。**翻转带的三维形状（管 vs 面）本轮只有间接证据，留待定向加密。**
E1 的 ±ε 翻转方向结论不在 Atlas 数据中重复声称。

### Obs-3（Gap-01，口径降级）：当前协议下的未访问/低访问区
在**当前协议**（各 ε 平衡态 → 单次质量守恒侵蚀 → 自由运行 T=300）下：
Formal 点 0.05 半径内无可达样本者占 96–98%（主体是"轨迹为零测度曲面"的平凡效应）；
非平凡部分：**A+B≥0.1 且 R<3.0 的带 4/5 个 R 段 ≥99.5% 未访问**（三个 ε 一致；
`gap_bands_eps*.csv`）。措辞限定：这是"当前协议下未访问/低访问区域"，
**不称"历史不可达/无法经过"**——绝对不可达需数学证明或更广历史协议验证。
候选解释（未验证）：质量恢复与 r 恢复经由 p=G·k·A·B·M 同步变化，
使"质量已恢复而 R 仍低"的状态难以在单次侵蚀历史中出现。

### Obs-4：可达包络随 ε 幅值缩放，形状不变
各 ε 不动点不同（见 §一），可达包络 A,B 上限 0.493/0.460/0.416、R 上限
4.486/4.299/4.019（+/0/−）；包络形状三 ε 一致，未观察到拓扑差异。

## 五、未观察到 / 不声称

未观察到：恢复区内部多稳态孤岛、可达集分叶、除 Boundary-01 外的第二结局边界、
随分辨率消失复现的瞬态结构。不声称：任何功能含义、任何认知/记忆/价值关联、
任何"不可达"的绝对断言。局限：T=150 依赖 §三 的时程复核；Map B 单次侵蚀协议
限制了对可达集的覆盖（Gap-01 的解释因此保持协议相对）；3D 切片仅取代表档位。

## 六、产物清单

`results/soul0_atlas0/`：formal_coarse_*.csv ×3、formal_medium_*.npz ×3、
reachable_*.npz ×3（含各 ε 不动点）、flip_surface_*.csv ×3、gap_bands_*.csv ×3、
atlas0_repro.json、atlas0_analysis.json、atlas_eps*.png ×3（重生成）、
atlas3d_eps*.html ×3（交互式 3D，可拖动/缩放，仅可视化）。
