# SOUL0_ATLAS0_FINDINGS.md — 状态空间图谱观察记录（第一阶段草稿）

> 2026-09-13。设计：SOUL0_ATLAS0_DESIGN.md。**只记录观察事实，不解释成认知、记忆、
> 价值、器官或主体结构。**区域使用无意义编号。产物：`results/soul0_atlas0/`
> （coarse CSV×3、medium npz×3、reachable npz×3、图×3；本次未动用自适应加密——
> 依据见 §三）。

## 一、执行规模

- Map A（Formal）：每 ε coarse 66,300 点 + medium 515,100 点（合法域直接采样，
  free-running T=150 分类，依据 E1 的 fates 时程稳定性）。
- Map B（Reachable）：每 ε 441 条合法历史（平衡态经 21×21 质量守恒侵蚀网格，
  r₀=运行值 4.2078），free-run T=300，记录访问样本 1,654,191/ε。
- R 采样上界按解析推导 R̂=k·G_sup/(27μ)：+0.25 → 6.944；0/−0.25 → 5.556
  （ε≤0 时 G_sup=1，因 tanh≥0）——三组分别采样 [0, 1.05·R̂]。

## 二、观察到的结构

### Obs-1：Formal 空间由一个大恢复区和两个消散区构成
恢复点占比（coarse/medium）：+0.25 → 90.7%/93.8%；0 → 89.7%/92.8%；−0.25 → 88.3%/91.0%。
两个消散区：**Region-D1** = A+B→1 的斜边薄板（M→0，再生底物枯竭）；**Region-D2** =
低于鞍点的低质量区（对称线附近 A,B ≲ a₁≈0.079，与 E0 解析鞍点一致）。
其余均为恢复区。未观察到恢复区内部的孤岛或镶嵌结构。

### Obs-2：命运翻转面是一条贴着鞍点的窄斜带，且随 ε 符号倾斜
(A+B, R) 分箱的翻转面扫描：清洁的 <0.5→≥0.5 翻转**只在 A+B≈0.14 附近出现**
（正处鞍点质量带；E1 的命运分岔带 ρ∈[0.82,0.845] 即 A+B≈0.151——两个独立实验
指向同一结构，记 **Boundary-01**）。带的位置随 R 倾斜：A+B≈0.14 处翻转 R≈4.69
（ε=+0.25）。ε=0 与 −0.25 的翻转带向低 R/更高质量侧移动（与 E1 的 ±ε 方向翻转一致）。
远离 Boundary-01 的区域，结局对 R 不敏感。

### Obs-3：coarse→medium 稳定性
多数结局一致率 0.89–0.90（三 ε），不一致集中在 Boundary-01 邻域；
体素级边界 Jaccard 仅 0.05–0.06——**这主要是体素尺寸伪影**（边界面元的体素集
随分辨率平方缩放且位置平移），不宜作为边界不稳定的证据；翻转面扫描（Obs-2）
是更可靠的定位方式，其位置在两分辨率下一致。恢复区/消散区的大区域结构
在两分辨率下稳定。**判定：主要结构趋稳；本轮不追加百万/千万级扫描**
（自适应加密未触发——无结局翻转的孤立邻域需要细化）。

### Obs-4（Gap-01）：历史可达集是薄曲面，"中等质量+低 R"长期不可达
Reachable 样本范围：A,B∈[0, 0.488]（永不越过 a₂+少量过冲），R∈[0.010, 4.36]
（深侵蚀后 r 衰减至近 0）。Formal 点在 0.05 半径内无可达样本者占 96–98%——
主体是"轨迹为零测度曲面"的平凡效应，但分带统计显示一个非平凡空区：
**A+B≥0.1 且 R<3.0 的带 99.5–100% 无历史访问**（低质量带亦 47–76%）。
即：**质量已恢复而调节水平仍低的状态（Gap-01）在合法域内大量存在，
但合法历史从不经过**——质量恢复与 r 恢复经由 p=G·k·A·B·M 锁死同步，
历史无法先补质量后补 r。此为 Formally-legal ≠ Reachable 的首个具体实例。

### Obs-5：可达集对 ε 的变化仅幅值级
三 ε 的可达包络形状一致，仅随 ε 符号整体缩放（A,B 上限 +ε 0.488 / 0 0.460 /
−ε 0.423；R 上限同理）。未观察到 ε 改变可达集拓扑的现象。

## 三、未观察到 / 本轮不声称

- 未观察到：恢复区内部的多稳态孤岛、可达集的分叶、随分辨率消失后又出现的
  瞬态结构、除 Boundary-01 外的第二条结局边界。
- 不声称：任何区域的功能含义、任何与认知/记忆/价值的关联；Atlas-0 只测绘几何。
- 局限：分类时程 T=150 依赖 E1 的时程稳定性结论；Map B 的侵蚀网格 21×21 决定
  可达面的采样密度（轨迹本身连续，占用统计对网格有依赖）；三维切片图仅取
  代表性档位。

## 四、下一步候选（仅列出，未决定）

1. Boundary-01 的面定向加密（只在该带内细分 (A+B, R)）；
2. Map B 增加侵蚀深度轴的分辨率以检验 Gap-01 是"从未"还是"罕见"；
3. 若需要，对 −ε 的调节有害带做与 +ε 对称的翻转面扫描。
